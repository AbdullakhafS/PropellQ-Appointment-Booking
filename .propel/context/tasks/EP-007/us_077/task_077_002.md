# Task 002: Implement HMAC Generation for Audit Entries

Task ID: task_077_002
User Story: US-077
Epic: EP-007
Priority: CRITICAL
Story Points: 1
Owner Role: Data Engineer
Status: Done

## Objective
Store an integrity hash alongside each audit log entry.

## Acceptance Criteria
- [x] Each new audit entry gets an integrity value.
- [x] Integrity metadata is stored safely.

## Definition of Done
Audit entry hashing is in place.

## Implementation
- `app/src/audit_storage.py` — `AuditEntry.integrity_hash` + `prev_chain_hash` fields; `AppendOnlyAuditStore.append()` sets HMAC hash at write time; `AuditIntegrityKeyService.key_summary()` exposes non-sensitive key metadata.
