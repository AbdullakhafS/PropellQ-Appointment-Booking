# Task 001: Define Admin Audit Query Access Pattern

Task ID: task_078_001
User Story: US-078
Epic: EP-007
Priority: HIGH
Story Points: 2
Owner Role: Backend / UI Architect
Status: Done

## Objective
Define the admin-only audit query surface, read-only behavior, and search parameters.

## Acceptance Criteria
- [x] Admin-only access is specified.
- [x] Supported filters and result fields are defined.
- [x] Read-only constraint is documented.

## Definition of Done
Query interface design is approved.

## Implementation
- `app/src/audit_storage.py` — `AUDIT_QUERY_ADMIN_ROLE = "admin"`, `AUDIT_EXPORT_FIELDS`, `AuditQueryParams` dataclass documents all supported filters (actor_id, actor_role, event, action, resource_type, resource_id, outcome, from_ts, to_ts), pagination, and sort parameters.
- `app/src/rbac.py` — `GET /api/admin/audit/query` and `GET /api/admin/audit/query/export` registered in permission map.
