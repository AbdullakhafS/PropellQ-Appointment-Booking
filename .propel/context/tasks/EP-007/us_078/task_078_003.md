# Task 003: Build Admin Audit Query UI and Export Flow

Task ID: task_078_003
User Story: US-078
Epic: EP-007
Priority: HIGH
Story Points: 1
Owner Role: Frontend Engineer
Status: Done

## Objective
Provide the admin-facing UI for filtering, selecting, and exporting audit records.

## Acceptance Criteria
- [x] Results render with detail views.
- [x] CSV and JSON export actions are available.

## Definition of Done
Admin audit UI is usable.

## Implementation
- `app/src/audit_storage.py` — `AuditQueryService.entry_detail()` returns full metadata with safe fallbacks; `export_csv()` and `export_json()` use `AUDIT_EXPORT_FIELDS` (policy-compliant).
- `app/src/web_app.py` — `GET /api/admin/audit/query/export?format=csv|json` route + `_handle_audit_query_export()` with `Content-Disposition` attachment response.
